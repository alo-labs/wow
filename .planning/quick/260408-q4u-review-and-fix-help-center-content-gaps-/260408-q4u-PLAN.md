# Quick Plan: Fix Help Center Content Gaps
**ID:** 260408-q4u  
**Created:** 2026-04-08  
**Scope:** Correct factual errors and fill content gaps in Help Center HTML files

---

## Affected Files

- `site/help/concepts/index.html` — agent table, domain framing, backup path, rollback command, auto-rollback claim, agent count, iteration loop, credentials/intake, hostinger-agent
- `site/help/optimization/index.html` — backup path, agent list (uses wrong friendly names), auto-rollback claim
- `site/help/reference/index.html` — agent table, backup path, rollback flag syntax, agent count in intro, skip-flag examples
- `site/help/getting-started/index.html` — agent count in intro, installation command context, credentials/intake mention
- `site/help/index.html` — agent count in hub description

---

## Task 1 — Fix `concepts/index.html` (core accuracy fixes)

**File:** `site/help/concepts/index.html`

**Issues addressed:** #1 (domain model framing), #2 (agent names/mapping), #3 (backup path), #4 (rollback command), #5 (auto-rollback claim), #6 (agent count), #7 (iteration loop), #8 (credentials/intake), #9 (hostinger-agent)

**Changes:**

1. **Domain section heading + intro (issue #1)**
   - Change heading from "The 5 Optimization Domains" to "The 5 Optimization Focus Areas"
   - Update intro paragraph to read: "WOW! organizes optimization work into five user-facing focus areas. Each focus area maps to one or more specialist agents in the internal execution model (Plugin, Provider, Custom, Theme, Content). These focus areas reflect what gets improved — the agents handle how."
   - Keep Performance / Security / SEO / Caching / Image as the five focus areas (they are correct for user-facing content)

2. **Agent count in TOC + section heading (issue #6)**
   - TOC `<li>` and `<h2>` text: "11 Specialist Agents" → "12 Specialist Agents"
   - Page subtitle (`<p>` under `<h1>`): "its 11 specialist agents" → "its 12 specialist agents"

3. **Agent table (issues #2, #9) — replace entire table body with:**

   | Friendly Name | Actual Agent File(s) | Responsibility |
   |---|---|---|
   | Orchestrator | (CLAUDE.md orchestration loop) | Sequences all agents; reruns until improvement < 5% |
   | Audit | lighthouse-agent + inventory-agent | Baseline performance scan + full plugin/theme/asset inventory |
   | Screenshot / Visual State | screenshot-agent | Captures before/after screenshots for regression comparison |
   | Theme Analysis | theme-analysis-agent | Detects theme-level performance and compatibility issues |
   | Planner | plan-agent | Generates the prioritized change plan from audit findings |
   | Backup | backup-agent | Full DB + files snapshot before any changes |
   | Plugin | plugin-agent | Audits and optimizes plugin configuration and load order |
   | Provider (incl. Hostinger) | provider-agent + hostinger-agent | Applies server-side and hosting-tier optimizations; Hostinger-specific tuning via hostinger-agent |
   | Custom | custom-agent | Applies custom code-level optimizations |
   | Visual Regression | visual-regression-agent | Compares before/after screenshots; flags regressions |
   | Report | report-agent | Generates the final HTML performance report |

   Add a note below the table: "hostinger-agent is a specialized sub-agent of the Provider tier, invoked automatically when a Hostinger environment is detected."

4. **Backup path (issue #3)**
   - `wp-content/wow-backups/` → `/tmp/.wow/backups/`

5. **Rollback command (issue #4)**
   - `<pre><code>/wow --rollback</code></pre>` → `<pre><code>/wow rollback</code></pre>`

6. **Auto-rollback claim (issue #5)**
   - Change: "If the Testing agent detects a visual regression after execution, WOW! automatically triggers a rollback to the snapshot taken in that run. No manual action is required."
   - To: "If the Visual Regression agent detects a regression after execution, it flags the run and records the regression in the report. You can then restore the pre-run backup manually with `/wow rollback`."

7. **Iteration loop (issue #7)**
   - In the "How It Works" / orchestration intro paragraph, add after the pipeline description: "WOW! runs the full pipeline in a loop, repeating until the measured performance improvement between cycles falls below 5%. This ensures compounding gains without over-optimization."

8. **Credentials/intake (issue #8)**
   - Add a new subsection "First-Run Setup" before the domains section, or expand the intro paragraph with: "On first run, WOW! prompts for the target site URL, WordPress admin credentials, and (optionally) SSH access details for server-side optimizations. These are stored encrypted in the local WP-CLI config for subsequent runs."

**Commit message:**
```
fix(help/concepts): correct agent names, count, backup path, rollback command, auto-rollback claim; add iteration loop and first-run setup docs
```

---

## Task 2 — Fix `optimization/index.html` (pipeline step accuracy)

**File:** `site/help/optimization/index.html`

**Issues addressed:** #3 (backup path), #5 (auto-rollback claim), #2 (wrong agent names in step 4 list)

**Changes:**

1. **Backup path (issue #3)**
   - All occurrences of `wp-content/wow-backups/` → `/tmp/.wow/backups/`

2. **Auto-rollback claim (issue #5)**
   - Change: "If a regression is detected, **automatic rollback** triggers immediately — restoring the backup from Step 3"
   - To: "If a regression is detected, the Visual Regression agent flags the run. You can restore the pre-run backup with `/wow rollback`."

3. **Step 4 agent list (issue #2) — replace the bullet list with accurate names:**
   - Remove: "CSS/JS agent", "Image agent", "Caching agent", "CDN agent", "Database agent", "Security agent", "SEO agent"
   - Replace with:
     - **Plugin agent** — audits and optimizes plugin configuration and load order
     - **Provider agent** — applies server-side and hosting-tier optimizations (auto-detects Hostinger via hostinger-agent)
     - **Custom agent** — applies custom code-level optimizations
     - **Theme Analysis agent** — resolves theme-level performance issues identified in the audit
   - Add note: "The specific optimizations applied depend on the audit findings from the Audit phase. Not all agents run on every site — only those with applicable findings are activated."

4. **Skip-flag examples in this file (if any)**
   - Update `--skip-image`, `--skip-database` examples → `--skip-plugin`, `--skip-provider`, `--skip-custom`

**Commit message:**
```
fix(help/optimization): correct backup path, rollback behavior, and agent names in pipeline steps
```

---

## Task 3 — Fix `reference/index.html` (CLI reference accuracy)

**File:** `site/help/reference/index.html`

**Issues addressed:** #3 (backup path), #4 (rollback command), #6 (agent count), #2 (agent table), #9 (hostinger-agent)

**Changes:**

1. **Intro text agent count (issue #6)**
   - "all 11 agent flags" → "all 12 agent flags"

2. **Backup path in agent reference table (issue #3)**
   - `wp-content/wow-backups/` → `/tmp/.wow/backups/`

3. **Rollback flag in command examples (issue #4)**
   - `/wow --rollback` → `/wow rollback` (wherever it appears as a command to run)
   - Keep `--rollback` only in the flags table row that describes the flag itself (since the flag may legitimately be named `--rollback` in the CLI schema)
   - Cross-check: if the flag table row says `--rollback` but README says `rollback` is a subcommand (not a flag), update the table row from flag to subcommand format

4. **Agent reference table (issues #2, #9) — replace with correct agents:**
   - Remove old rows for CSS/JS, Database, CDN, Testing agents
   - Add rows for: lighthouse-agent, inventory-agent, screenshot-agent, theme-analysis-agent, plan-agent, backup-agent, plugin-agent, provider-agent, hostinger-agent, custom-agent, visual-regression-agent, report-agent
   - Group hostinger-agent as a sub-row or note under provider-agent: "hostinger-agent — invoked by provider-agent on Hostinger environments"

5. **Skip-flag examples in command table**
   - Update `--skip-image`, `--skip-seo`, `--skip-database` → `--skip-plugin`, `--skip-provider`, `--skip-custom`, `--skip-backup`

6. **`wow.config.json` example schema**
   - Remove `"agents": { "image": ... }` keys that reference non-existent agents
   - Update `"domains"` key comment: clarify it controls Lighthouse scan targets, not WOW's internal execution domains

**Commit message:**
```
fix(help/reference): correct agent table, backup path, rollback subcommand, skip-flag examples, agent count
```

---

## Task 4 — Fix `getting-started/index.html` and `index.html` (counts + intake + install context)

**Files:** `site/help/getting-started/index.html`, `site/help/index.html`

**Issues addressed:** #6 (agent count), #8 (credentials/intake), #10 (installation command context)

**Changes to `getting-started/index.html`:**

1. **Agent count (issue #6)**
   - "11 specialist agents" → "12 specialist agents" (in intro/overview paragraph, line ~86)

2. **Credentials/intake (issue #8)**
   - After the installation step and before the first-run step, add a "First Run" subsection:
     ```
     On first run, WOW! walks you through a short setup:
     - Site URL (e.g., https://yourdomain.com)
     - WordPress admin username and password
     - SSH access (optional — enables server-side optimizations)

     These are stored locally in your WP-CLI config (never transmitted).
     ```

3. **Installation command context (issue #10)**
   - The file already shows `wp package install alo-labs/wow`. Add a clarifying note:
     ```
     <!-- Add inline note after the wp package install line -->
     Alternatively, if you're inside a WOW! session: /plugin install alo-labs/wow
     ```
   - Display both forms with context labels: "From terminal:" and "From WP-CLI session:"

**Changes to `site/help/index.html`:**

1. **Agent count in hub card/description (issue #6)**
   - Find any "11 agents" references → "12 agents"
   - Specifically in the concepts card description: "its 11 specialist agents" → "its 12 specialist agents"

**Commit message:**
```
fix(help/getting-started,index): correct agent count, add first-run credentials intake, clarify install command context
```

---

## Execution Order

Run tasks in sequence (each is one commit):

1. Task 1 — `concepts/index.html` (foundational definitions, do first)
2. Task 2 — `optimization/index.html` (pipeline content, references concepts)
3. Task 3 — `reference/index.html` (CLI reference, most cross-linked)
4. Task 4 — `getting-started/index.html` + `index.html` (surface-level count fixes, last)

---

## Verification Checklist

After all tasks complete, grep-verify the following are gone from the entire `site/help/` tree:

```bash
# These strings must NOT appear anywhere in site/help/ after fixes:
grep -r "wow-backups" site/help/         # should return nothing
grep -r "/wow --rollback" site/help/     # should return nothing
grep -r "11 agent\|11 specialist" site/help/  # should return nothing
grep -r "automatically triggers a rollback" site/help/  # should return nothing
grep -r "CSS/JS agent\|Database agent\|CDN agent\|Testing agent" site/help/  # should return nothing
grep -r "5 domains\|five domains" site/help/   # should return nothing (now "focus areas")

# These strings MUST appear after fixes:
grep -r "/tmp/.wow/backups" site/help/   # backup path
grep -r "/wow rollback" site/help/       # correct rollback subcommand
grep -r "12 specialist\|12 agents" site/help/  # correct count
grep -r "hostinger-agent" site/help/     # documented
grep -r "improvement.*5%" site/help/     # iteration loop
grep -r "credentials\|site URL\|SSH" site/help/getting-started/  # intake
```
